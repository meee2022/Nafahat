/**
 * ⚙️ معلومات التطبيق - ملف الإعدادات المركزي.
 *
 * عدّل أي قيمة هنا وستنعكس تلقائياً في:
 *   - شاشة "عن التطبيق" (روابط، بريد، شعار)
 *   - الفوتر (السنة، صانعي التطبيق)
 *   - رسائل المشاركة
 *
 * 💡 كل القيم قابلة للتعديل بحرية. اللي مش عاوزه (مثلاً GitHub لو مش جاهز)
 *    خليه empty string `''` وهيختفي تلقائياً من الشاشة.
 */

export const APP_INFO = {
  // ───── الهوية ─────
  /** الاسم العربي للتطبيق. */
  name:        'نَفَحات',
  /** الاسم الإنجليزي (للمتاجر، النطاق). */
  nameEn:      'Nafahat',
  /** الشعار/الجملة التعريفية. */
  tagline:     'صحبة دائمة مع القرآن الكريم',
  /** نسخة التطبيق. */
  version:     '1.0.0',
  /** رقم البناء. */
  build:       '100',

  // ───── الفوتر ─────
  /** السنة المعروضة في الـ copyright. اتركه null لاستخدام السنة الحالية تلقائياً. */
  copyrightYear: null as number | null,
  /** نص "صُنع بـ ❤️ لـ ..." - الجزء الأخير فقط. */
  madeWithSuffix: 'لأهل القرآن',
  /** إهداء/توضيح أن التطبيق صدقة جارية - اتركه '' لإخفاء البند. */
  charityNotice: 'تم عمل هذا التطبيق من قِبَل فاعل خير - صدقة جارية',

  /**
   * تفاصيل إهداء الصدقة الجارية - تظهر فقط في شاشة "عن التطبيق".
   * اتركه null لإخفاء البطاقة كاملة.
   */
  charityDedication: {
    /** العنوان الرئيسي للبطاقة. */
    title: 'صدقة جارية',
    /** قسم الوالدين/الأحياء بالاسم. */
    parents: {
      /** التمهيد قبل ذكر الأسماء. */
      label: 'لوالديّ',
      /** الأسماء (قابلة للإضافة/الحذف). */
      names: 'إبراهيم شاهين ونشوى الشحات',
      /** دعاء بعد الأسماء. */
      prayer: 'أطال الله في عمرهما',
    },
    /** قسم العموم - بدون أسماء. */
    general: {
      /** الجملة الأولى - الشمولية. */
      title: 'ولكل من له حقٌّ علينا',
      /** الوصف. */
      scope: 'من أحياء وأموات',
      /** الدعاء للمتوفّين. */
      deceasedPrayer: 'رحم الله من رحلوا منهم',
      /** الدعاء للأحياء. */
      livingPrayer: 'وحفظ من بقي',
    },
  } as null | {
    title: string;
    parents: { label: string; names: string; prayer: string };
    general: { title: string; scope: string; deceasedPrayer: string; livingPrayer: string };
  },

  // ───── روابط التواصل ─────
  /** بريد الدعم الفني. اتركه '' لإخفاء البند. */
  supportEmail: 'support@nafahat.app',
  /** الموقع الإلكتروني (بدون https://). اتركه '' لإخفاء البند. */
  website:      'nafahat.app',
  /** رابط مستودع GitHub الكامل. اتركه '' لإخفاء البند. */
  githubUrl:    'https://github.com',
  /** نص يظهر تحت "مصدر مفتوح على GitHub". */
  githubLabel:  'contribute to our codebase',

  // ───── روابط المتاجر ─────
  /** رابط التطبيق على App Store. */
  appStoreUrl:  'https://apps.apple.com/',
  /** رابط التطبيق على Google Play. */
  playStoreUrl: 'https://play.google.com/',

  // ───── روابط قانونية ─────
  /** رابط شروط الاستخدام. */
  termsUrl:     'https://nafahat.app/terms',
  /** رابط سياسة الخصوصية. */
  privacyUrl:   'https://nafahat.app/privacy',

  // ───── رسالة المشاركة ─────
  /** النص اللي بيظهر لما المستخدم يشارك التطبيق مع صحابه. */
  shareMessage: 'نَفَحات - صحبة دائمة مع القرآن الكريم 🌙\nحمّل التطبيق الآن.',
} as const;

/** السنة المعروضة في الـ copyright (تلقائية أو محدّدة). */
export function getCopyrightYear(): number {
  return APP_INFO.copyrightYear ?? new Date().getFullYear();
}
